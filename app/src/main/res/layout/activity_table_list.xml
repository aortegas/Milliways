<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.ActivityTableList">

    <!-- We load dinamic the fragment in ActivityTableList with id of FrameLayout. -->
    <FrameLayout
        android:id="@+id/activity_table_list_frame_list_id"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
    </FrameLayout>

    <!-- We load dinamic the fragment in ActivityTableList with id of FrameLayout. -->
    <FrameLayout
        android:id="@+id/activity_table_list_frame_button_id"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_anchor="@+id/activity_table_list_frame_list_id"
        app:layout_anchorGravity="bottom|end|right">
    </FrameLayout>

</android.support.design.widget.CoordinatorLayout>